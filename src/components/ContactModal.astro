<script>
    const TALLY_KEY = 'tly-xX6NNk';
    const TALLY_URL = 'https://tally.so/r/xX6NNk';

    window.addEventListener('toggle-modal', () => {
        if (typeof Tally !== 'undefined') {
            Tally.openPopup(TALLY_KEY);
        } else {
            window.open(TALLY_URL, '_blank', 'noopener');
        }
    });
</script>